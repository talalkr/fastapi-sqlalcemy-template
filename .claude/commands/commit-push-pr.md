Follow the instructions in `.claude/skills/app-commit/SKILL.md` to generate a commit message and commit staged changes.

Then follow the instructions in `.claude/skills/app-pr-description/SKILL.md` to generate a PR description, push the branch, and open a PR.

Arguments (optional): $ARGUMENTS
